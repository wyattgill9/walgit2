name: CI

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@main

      # - name: Install fix
      #   env:
      #     FIX_VERSION: v0.3.0
      #   run: |
      #     curl -sSfL "https://github.com/psyclyx/fix/releases/download/$FIX_VERSION/fix-$FIX_VERSION-x86_64-linux.tar.gz" \
      #       | tar xz -C /usr/local --strip-components=1

      - name: Evaluate
        run: nix flake check -L
        # fix:
        # run: fix flake check --extra-experimental-features flakes
        # evix:
        # run: |
        #   for root in checks packages devShells; do
        #     nix run github:manic-systems/evix -- eval \
        #       --flake ".#$root" --force-recurse \
        #       --workers "$(nproc)" \
        #       > "jobs-$root.ndjson"
        #   done
        #   if jq -e 'select(has("error"))' jobs-*.ndjson; then
        #     echo "::error::flake evaluation failed" && exit 1
        #   fi

      - name: Build
        run: nix build -L
        # evix:
        # run: |
        #   mapfile -t drvs < <(jq -r --arg s "$SYSTEM" 'select(.system == $s) | .drvPath + "^*"' jobs-checks.ndjson jobs-packages.ndjson)
        #   [ "${#drvs[@]}" -gt 0 ] || { echo "::error::nothing evaluated for $SYSTEM" && exit 1; }
        #   nix build -L --keep-going "${drvs[@]}"
